# Django Secret Key (Generate your own using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")
SECRET_KEY=your-secret-key-here-generate-a-new-one

# Debug Mode (set to False in production)
DEBUG=True

# Allowed Hosts (comma separated, leave empty for default)
ALLOWED_HOSTS=
